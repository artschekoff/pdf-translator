After code changes, at minimum run targeted package tests. Prefer running `go test ./...` when feasible. Renderer changes should be validated with `go test ./internal/renderer`. Note: current repository has pre-existing failing tests in internal/extractor that assume a different block ordering; these failures are unrelated to renderer changes and appear when running the full suite.