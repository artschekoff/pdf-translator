Project language is Go. Tests use the standard testing package with testify assert/require. Internal packages are organized by responsibility (extractor, renderer, pipeline, translator, etc.). Keep changes pragmatic and localized; renderer logic relies on geometric heuristics over domain.TextBlock bounding boxes. Prefer Go idioms, small helper functions, and focused package tests.